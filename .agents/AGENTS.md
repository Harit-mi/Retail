# Antigravity Workspace Rules — OmniRoute AI Router Integration

- **AI Gateway Endpoint**: Use `diegosouzapw/OmniRoute` (`http://localhost:20128/v1`) for unified model routing across Gemini, Claude, GPT, and DeepSeek.
- **Model Fallback**: Enable quota-aware auto-fallback when running complex subagent tasks.
- **Prompt Token Compression**: Utilize RTK/Caveman token compression for long context runs.
